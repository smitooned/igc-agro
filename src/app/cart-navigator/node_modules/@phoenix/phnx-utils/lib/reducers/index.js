"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = rootReducer;

var _redux = require("redux");

var currentReducers;

function rootReducer(newReducer) {
  var isRemoval = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : false;
  currentReducers = currentReducers || {
    _: function _() {
      var state = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {};
      var action = arguments.length > 1 ? arguments[1] : undefined;
      return state;
    }
  };

  if (newReducer) {
    if (!isRemoval) {
      currentReducers[newReducer.reducerName] = newReducer.reducer;
    } else {
      delete currentReducers[newReducer.reducerName];
    }
  }

  return (0, _redux.combineReducers)(currentReducers);
}